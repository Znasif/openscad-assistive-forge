/**
 * Vite Configuration for OpenSCAD Customizer (Vue)
 * @license GPL-3.0-or-later
 */

import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    open: true,
  },
  build: {
    target: 'es2015',
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'three': ['three'],
        },
      },
    },
  },
  optimizeDeps: {
    include: ['three'],
  },
  worker: {
    format: 'es',
  },
});
