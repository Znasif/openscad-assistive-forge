# Cloudflare Pages Configuration
# This file is optional for Pages deployments via GitHub integration
# If using wrangler CLI directly: npx wrangler pages deploy dist

name = "openscad-assistive-forge"
pages_build_output_dir = "dist"
